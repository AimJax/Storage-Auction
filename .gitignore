# Build outputs / Rojo
build/
*.rbxl
*.rbxlx
*.rbxl.lock
*.rbxm
sourcemap.json

# Places: local-only world files (code AND map source-of-truth live here).
# The versionable map representation is map/ (tracked). Never commit .rbxl.
# (StorageAuction-place-before-bidirectional-sync.rbxl is the pre-sync backup.)

# Argon map sync: map/ is INTENTIONALLY TRACKED — do not ignore it.
# Argon writes only under map/ per map.project.json; nothing else to exclude.

# Rojo plugin artifacts
rojo-plugin/

# Logs
*.log
D:/Roblox/Logs/

# OS / editor junk
.DS_Store
Thumbs.db
*.tmp
*.bak
~$*

# VSCode
.vscode/*

# Secrets — never commit these
*.pem
*.key
.env
*token*
*secret*

# Aftman / Rokit toolchain (binaries live in D:/Roblox/Dev, not here)
# Intentionally NOT ignored if we pin versions per-project:
# (keep aftman.toml / rokit.toml committed, ignore downloaded binaries)
.toolchain/
